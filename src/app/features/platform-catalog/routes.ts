import { Routes } from '@angular/router';
import { PlatformCatalogComponent } from './platform-catalog.component';

export const platformCatalogRoutes: Routes = [
  { path: '', component: PlatformCatalogComponent },
];
